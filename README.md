# Sentinel-Report
A **root-only**, **read-only** Linux triage script that produces a single **HTML incident snapshot** (plus a raw log) to help you quickly spot suspicious network activity, persistence, and access on an Ubuntu host.
